; (ocamllex
;  (modules ulexer))

; (menhir
;  (modules uparser)
;  (flags
;   (-v --explain)))

(executables
  (flags (:standard -w -26-27-32-33))
  (promote (until-clean))
  (names loop main)
  (libraries camlp-streams))
